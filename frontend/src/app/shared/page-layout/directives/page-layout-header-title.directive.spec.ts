import { PageLayoutHeaderTitleDirective } from './page-layout-header-title.directive';

describe('PageLayoutHeaderTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new PageLayoutHeaderTitleDirective();
    expect(directive).toBeTruthy();
  });
});
