import { PageLayoutHeaderSubtitleDirective } from './page-layout-header-subtitle.directive';

describe('PageLayoutHeaderSubtitleDirective', () => {
    it('should create an instance', () => {
        const directive = new PageLayoutHeaderSubtitleDirective();
        expect(directive).toBeTruthy();
    });
});
