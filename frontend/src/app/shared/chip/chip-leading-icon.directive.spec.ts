import { ChipLeadingIconDirective } from './chip-leading-icon.directive';

describe('ChipLeadingIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipLeadingIconDirective();
    expect(directive).toBeTruthy();
  });
});
