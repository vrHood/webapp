import { OverviewTableDirective } from './overview-table.directive';

describe('OverviewTableDirective', () => {
    it('should create an instance', () => {
        const directive = new OverviewTableDirective();
        expect(directive).toBeTruthy();
    });
});
