import { DialogActionsDirective } from './dialog-actions.directive';

describe('DialogActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogActionsDirective();
    expect(directive).toBeTruthy();
  });
});
