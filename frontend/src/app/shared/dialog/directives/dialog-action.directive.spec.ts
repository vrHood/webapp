import { DialogActionDirective } from './dialog-action.directive';

describe('DialogActionDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogActionDirective();
    expect(directive).toBeTruthy();
  });
});
