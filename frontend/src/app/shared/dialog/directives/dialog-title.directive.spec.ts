import { DialogTitleDirective } from './dialog-title.directive';

describe('DialogTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogTitleDirective();
    expect(directive).toBeTruthy();
  });
});
