import { RetailerInfoCloseButtonDirective } from './retailer-info-close-button.directive';

describe('RetailerInfoCloseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RetailerInfoCloseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
